Create a model of the object shown in the pictures. 

The dimensions of the individual details are at your discretion. 

The final model variable should be named solid. 

Use STL file as referrence and to compare the results

After you have created the CadQuery script and verified that the model is generated correctly, export the final model in STL format. 

Name the exported file Task2.stl.
